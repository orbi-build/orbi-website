# Agent instructions

Before changing a website-to-Cloud handoff, read the [beta Cloud endpoint context](docs/cloud-endpoints.md).

Use only the verified beta endpoint recorded there. Never guess, create, or replace a Cloud hostname; if the context has no verified value, stop and report the blocker.
