{
  "name": "orbi-website-worker",
  "private": true,
  "version": "1.0.0",
  "description": "orbi.build Cloudflare Worker + assets",
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@datafast/ai-crawl": "^1.0.9"
  },
  "devDependencies": {
    "vitest": "^5.0.0"
  }
}
